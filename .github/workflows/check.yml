# Every template is a valid KB fragment: directional manifests, schema tests,
# and registry freshness. Hosted execution is an explicit metered fallback.
name: check
on:
  workflow_dispatch:
    inputs:
      hosted:
        description: Use a metered GitHub-hosted runner as a manual fallback
        required: false
        type: boolean
        default: false
  pull_request:
  push:
    branches: [main]
jobs:
  check:
    runs-on: ${{ fromJSON(inputs.hosted && '["ubuntu-latest"]' || '["self-hosted","linux","x64","kyyn-templates-ci"]') }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Install the pinned toolchain (rust-toolchain.toml)
        run: rustup show active-toolchain
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        if: ${{ inputs.hosted == true }}
        with:
          workspaces: |
            blank/schema
            gbrain/schema
      - run: make check
