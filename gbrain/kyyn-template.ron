// A kyyn KB template (ADR 0007). gbrain carries no identity values at all —
// its charter ships as a starting point ("Make this your own") and the
// declared git-repo source points at the public gbrain spec repository.
(
    template: 1,
    name: "gbrain",
    summary: "A namespaced gbrain-style operational knowledge brain with a reviewed repository-processing recipe, typed relationship edges, and provenance-bearing takes.",
    substitutions: [],
)
